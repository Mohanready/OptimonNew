package com.optimon.repo;

import org.springframework.data.jpa.repository.JpaRepository;

import com.optimon.entities.EndpointDetails;


public interface EndpointDetailsRepository extends JpaRepository<EndpointDetails, Integer>{

}
